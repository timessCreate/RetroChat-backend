server:
  port: 9999
  servlet:
    context-path: /retrochat

spring:
  application:
    name: retrochat
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/retrochat-database?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: '099636'
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
      pool-name: code-agent-pool
      leak-detection-threshold: 15000
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8


